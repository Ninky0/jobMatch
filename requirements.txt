# FastAPI and related packages
fastapi==0.109.2
uvicorn==0.27.1
pydantic==2.6.1
python-multipart==0.0.9
httpx==0.26.0

# vLLM and related packages
vllm==0.3.3
torch==2.2.0
transformers==4.37.2

# Embedding and text processing
sentence-transformers==2.5.1
keybert==0.7.0
konlpy==0.6.0
numpy==1.26.3
dill==0.3.8

# Utility packages
tqdm==4.66.1
python-dotenv==1.0.1
pytest==8.0.0
pytest-asyncio==0.23.5

# Note: nginx is not included as it should be installed at the system level
# Note: Java is required for konlpy, should be installed at the system level 